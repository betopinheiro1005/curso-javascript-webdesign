export interface LoadMappingOptions {
    origValues?: boolean;
}
export interface Mapping {
    attributeSelectors?: string[];
    selectors?: {
        [s: string]: string;
    };
}
declare const load: (mapping: Mapping, options?: LoadMappingOptions) => void;
export default load;
