import { Attribute } from 'parse5';
export interface Attr extends Attribute {
    val: string;
}
declare const shouldTriggerAttribute: (attr: Attr, item: string | RegExp) => boolean;
export default shouldTriggerAttribute;
