"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

function arrayToRegex(values, modifier = value => value) {
  if (!values.length) {
    return null;
  }

  return new RegExp(`(${values // sort by size
  .sort((a, b) => b.length - a.length) // add -- to ensure to only capture attributes
  .map(modifier).join(')|(')})`, 'g');
}

exports.default = arrayToRegex;
module.exports = exports.default;